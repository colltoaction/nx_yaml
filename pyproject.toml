[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "nx_yaml"
version = "1.0.0"
description = "YAML as a human-readable Hypergraph Interchange Format (HIF) for NetworkX"
readme = "README.md"
dependencies = [
  "pyyaml",
  "nx_hif==0.0.11",
]

[project.urls]
"Source" = "https://github.com/colltoaction/nx_yaml"
