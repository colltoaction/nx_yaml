[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "nx_yaml"
version = "0.1.0"
description = "A higher-order syntax"
dependencies = [
  "pyyaml",
  "networkx",
]

[project.urls]
"Source" = "https://github.com/colltoaction/nx_yaml"
